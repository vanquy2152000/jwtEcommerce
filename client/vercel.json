{
  "build": {
    "env": {
      "CI": "true",
      "NEXT_TELEMETRY_DISABLED": "1"
    },
    "command": "npm install --force && npm run build",
    "rewrites": [
      {
        "source": "/(.*)",
        "destination": "/"
      }
    ]
  }
}